# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a GitHub Pages site deployed via Jekyll. The repository is currently in its initial state with placeholder files.

## Deployment

- **Hosting**: GitHub Pages
- **Build**: Jekyll, deployed via `.github/workflows/jekyll-gh-pages.yml`
- **Custom domain**: configured via `CNAME` (currently empty)

## Development

To serve locally:
```bash
bundle exec jekyll serve
```

No build tools, linters, or test frameworks are configured yet.
